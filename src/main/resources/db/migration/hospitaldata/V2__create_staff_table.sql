CREATE TABLE IF NOT EXISTS migrations.STAFF (
                                                 id INT2,
                                                 name TEXT NOT NULL,
                                                 uuid TEXT NOT NULL,
                                                 registration_date DATE NOT NULL,
                                                 PRIMARY KEY (id)
    );


